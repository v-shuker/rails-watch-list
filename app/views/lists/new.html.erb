<div class="container mt-5">
  <h2>Add a new list to your collection</h1>
  <%= simple_form_for @list do |f| %>
    <%= f.input :name %>
    <%# <%= f.input :photo, as: :file %>
    <%= f.submit class: 'btn btn-primary' %>
  <% end %>
</div>
